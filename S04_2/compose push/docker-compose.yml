 version: "3.3"

 services:
   api-students:
     build: ./api-students
     image: gcolchado/api-students
     ports:
       - 8000:8000
   websimple:
     build: ./websimple
     image: gcolchado/websimple
     ports:
       - 8080:80
